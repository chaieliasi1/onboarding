# הקמת מערכת החוזה והשאלון

פעם אחת, בערך 10 דקות. אחרי זה הכל עובד לבד.

## מה יש בתיקייה

| קובץ | מה זה |
|---|---|
| `admin.html` | עמוד הניהול שלך. סיסמה, שם, סכום, "צור קישור". |
| `c.html` | העמוד שהלקוח מקבל: חוזה, חתימה, שאלון, אישור פרטיות. |
| `api.js` | החיבור לגוגל. יש בו שורה אחת שצריך למלא (שלב 4). |
| `style.css` | העיצוב. |
| `apps-script/Code.gs` | "המנוע" שרץ אצלך בחשבון הגוגל. כאן גם נוסח החוזה ונוסח הפרטיות. |

## שלב 1: גוגל שיט + הדבקת הקוד

1. נכנסים ל-[sheets.new](https://sheets.new) (מחובר לחשבון eliasichai@gmail.com).
2. נותנים שם, למשל "לקוחות ליווי".
3. בתפריט: **Extensions → Apps Script**.
4. מוחקים את מה שיש בעורך, ומדביקים את **כל** התוכן של `apps-script/Code.gs`.
5. שומרים (אייקון הדיסקט).

## שלב 2: סיסמת ניהול

1. בעורך Apps Script, בצד שמאל: **Project Settings** (גלגל שיניים).
2. למטה: **Script Properties → Add script property**.
3. Property: `ADMIN_PASSWORD`. Value: סיסמה שרק אתה יודע (לפחות 10 תווים).
4. **Save script properties**.

## שלב 3: הרצה ראשונה ופרסום

1. חוזרים לעורך (אייקון `< >`). ברשימה למעלה בוחרים את הפונקציה `setup` ולוחצים **Run**.
2. גוגל יבקש הרשאות (גיליונות, דרייב, מסמכים, ג'ימייל). מאשרים.
   אם מופיע "Google hasn't verified this app": **Advanced → Go to ... (unsafe)**. זה הקוד שלך, בחשבון שלך.
3. למעלה מימין: **Deploy → New deployment**.
4. בגלגל השיניים ליד "Select type" בוחרים **Web app**.
5. Execute as: **Me**. Who has access: **Anyone**.
6. **Deploy**, ומעתיקים את ה-**Web app URL** (מסתיים ב-`/exec`).

## שלב 4: חיבור העמודים לגוגל

פותחים את `api.js` ומדביקים את הכתובת בשורה השנייה:

```js
window.ONBOARDING_API = 'https://script.google.com/macros/s/.../exec';
```

(או שולחים לי את הכתובת ואני אדביק.)

## שלב 5: העלאה לאוויר

מעלים את כל התיקייה `onboarding` לאחסון סטטי (GitHub Pages, Netlify וכו'). אפשר לבקש ממני לעשות את זה.
הכתובת של עמוד הניהול תהיה `.../admin.html`. כדאי לשמור אותה כמועדף בטלפון.

## בדיקה ראשונה

1. נכנסים ל-`admin.html`, יוצרים לקוח בדיקה עם השם שלך.
2. פותחים את הקישור, ממלאים את **המייל שלך**, חותמים, ממלאים שאלון, מאשרים פרטיות, שולחים.
3. בודקים:
   - הגיע אליך מייל עם 2 קבצי PDF (חוזה + שאלון).
   - הגיע אליך מייל "לקוח" עם החוזה בלבד.
   - בדרייב יש תיקייה "חוזים ושאלונים" עם הקבצים.
   - בשיט, בלשונית `clients`, השורה עברה ל-`done`.
4. מוחקים את שורת הבדיקה ואת הקבצים שלה.

## שימוש יומיומי

סוגרים עם לקוח → `admin.html` → שם + סכום → "צור קישור" → "שליחה בוואטסאפ".
הסטטוס ברשימה: **נשלח** (עוד לא חתם), **חתם, לא סיים שאלון**, **סיים** (עם קישורים לקבצים).
אם לקוח חתם ולא סיים את השאלון, אותו קישור ממשיך אצלו ישר לשאלון. את החוזה החתום כבר אפשר לפתוח מהרשימה.

## שינויים בעתיד

- **נוסח החוזה או הפרטיות:** עורכים ב-`Code.gs` (בראש הקובץ), ואז **Deploy → Manage deployments → ✏️ → Version: New version → Deploy**. הכתובת נשארת אותה כתובת.
- **שאלות בשאלון:** ב-`c.html`, ברשימה `Q`.
- **מייל אחר לקבלת ההתראות:** Script Property בשם `NOTIFY_EMAIL`.

## חשוב לזכור

- **מחיקה אחרי חצי שנה:** ברובריקת הפרטיות כתוב שהמידע נמחק חצי שנה אחרי סיום הליווי. המחיקה לא אוטומטית. כדאי לקבוע תזכורת ביומן לכל לקוח, ואז למחוק את הקבצים שלו מהתיקייה ואת השורה שלו מהשיט.
- **הקישור ללקוח הוא המפתח:** מי שיש לו את הקישור יכול לחתום. שולחים אותו רק ללקוח.
- **10 סיסמאות שגויות** בעמוד הניהול נועלות אותו לרבע שעה.
- זה לא ייעוץ משפטי. כדאי שעורך דין יעבור על נוסח ההסכם ועל רובריקת הפרטיות.
